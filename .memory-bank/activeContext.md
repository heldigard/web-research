# Active Context

## 2026-07-04 — SHIPPED
Graduation complete: `~/web-research/` vertical-slice project, public at
github.com/heldigard/web-research (commit `ac564a6`). Ecosystem reconnected via
`~/.claude/scripts/web-research.py` shim (config commit `4bcbc80`). 37 tests
green, ruff clean. See progress.md for the full milestone list.

## Handoff Format
When ending a session, run `project-memory handoff` and paste the output here.
