# Current Task
> Updated: 2026-07-05

## Goal
None active. Architecture refactor shipped (see progress.md).

## Next likely work
- cheap_llm.py graduation (separate future project — 33KB, 8+ consumers).
- Live-model re-bench if OLLAMA_SYNTH_MODEL / OLLAMA_EMBED change.
- Optional: ship `httpx` HttpClient impl behind a feature flag.
- Optional: extract `source_quality_score` domain list to external config.
