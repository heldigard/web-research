# Progress

## 2026-07-04 — Graduation milestone (SHIPPED)
- Scaffolded `~/web-research/` (pyproject, .gitignore, shim.py, LICENSE, CLAUDE.md, README.md). ✅
- Copied flat package verbatim into `src/web_research/` + migrated test suite. ✅
- Added `ECOSYSTEM_SCRIPTS` to `shared/config.py`; fixed `ollama_api.py` + `synthesis/engine.py` path injection (replaced broken `parent.parent` trick). ✅
- Baseline green (37 tests) against the new location. ✅
- Restructured into vertical slices: `features/{search,read,research,ranking,intelligence,synthesis}/` + `shared/`. ✅
- Extracted 3 mode-handlers from `cli.py` (255 LOC) into per-feature `command.py`; slimmed `cli.py` to ~28 LOC. ✅
- Added module aliases (`wr.search`/`wr.reader`/`wr.synthesis`) in `__init__.py` for test-compat; updated 3 test import paths. ✅
- All 37 tests green against the NEW package (resolved the stale `~/.claude/scripts` sys.path block that was shadowing the new package in the baseline — corrected). ✅
- ruff check + format clean (29 files). ✅
- Rewrote `~/.claude/scripts/web-research.py` shim → `WEB_RESEARCH_HOME`-based (mirrors codeq). ✅
- Added `~/.local/bin/web-research` symlink. ✅
- Removed old flat package + old test; committed in `~/.claude` as `4bcbc80`. ✅
- harness self-tests pass: verify-hooks 0 errors, test-rtk-config 71/71. ✅
- Init + seeded this memory bank. ✅
- Created github.com/heldigard/web-research (public), pushed. ✅

## Format
- [YYYY-MM-DD]: What was done + verification status
- 2026-07-05T02:12:16Z | status:completed | session:97300642-77bb-4a6e-981c-8ef060ef7e95 | claude: session done
- 2026-07-05T19:25:55Z | 2026-07-05: Improved web-research smart flow for cross-CLI LLM agents. Smart search/research now builds a bounded query set from query_profile expansions + preferred site filters; search_backends accepts optional internal query variants while keeping the public result contract and canonical URL dedup removes fragments/tracking params. Synthesis now enforces WEB_SYNTH_MAX_CONTEXT_CHARS (default 14000) and truncates source text before local/cloud model calls to reduce token spend without changing read/search output. Verified pytest -q (56 passed) and ruff check .; codescan all reported 0 leaks then failed in its own SAST wrapper (Namespace.config missing).
- 2026-07-05T20:00:48Z | 2026-07-05: Added canonical shims/web-research.py plus tests/test_shim.py and restored live ~/.claude/scripts/web-research.py + no-suffix web-research entrypoints. Isolated-HOME E2E and pytest+ruff passed.
- 2026-07-05T22:30:00Z | status:completed | session:e2ab894 | Architecture refactor SHIPPED. (1) chore(ci) e2ab894: added ruff format check + mypy strict + 85% coverage gate to .github/workflows/ci.yml; annotated one pre-existing sections: list[str] mypy error in synthesis/engine. (2) refactor(shared) 5e2fb2d: shared/config.py → frozen Settings dataclass + env loader + legacy SCREAMING_CASE proxy + get_settings/reload_settings API; externalized MINIMAX_URL, ZAI_SEARCH_URL, ZAI_READER_URL. shared/http.py → HttpClient Protocol + UrllibHttpClient default + default_client()/set_default_client() swap (future httpx is one setter call) + nosemgrep rationale on urlopen. shared/cache.py → schema-versioned entries (SCHEMA_VERSION + optional engine_tag), auto-invalidates on model/prompt change. (3) refactor(search) a4eae5b: features/search/engine.py → thin dispatcher; new features/search/backends/{base,searxng,minimax,zai}.py package (one file per backend, ~65-95 LOC). (4) refactor(read) cea2e49: features/read/engine.py → thin dispatcher with fallback chain; new features/read/backends/{base,firecrawl,zai_reader}.py. (5) test 26f964a: 3 legacy patches retargeted to wr.search.backends.{minimax,zai} + new BackendSliceTests class asserting build_backend/normalize_url/SearchResult.to_dict/Settings reload/cache invalidation without HTTP. (6) docs 68941e3: ARCHITECTURE.md describing vertical-slice layout + 3-step recipes for new backends + HttpClient swap procedure + schema-version cache contract. Verified: ruff format+lint clean, mypy clean, pytest 63/63 pass, coverage 89% (gate 85%), codescan dead=0, codescan secrets=0, live SearXNG smoke returns real Rust-2026 results.
- 2026-07-06T19:30:00Z | status:completed | cheap_llm decoupling adoption (2026-07-06): (1) shared/config.py — removed `or _env_str("CHEAP_LLM_HOME")` from `ecosystem_scripts` resolution; CHEAP_LLM_HOME points at the cheap-llm PROJECT ROOT (has cheap_llm.py but NOT ollama_client.py), so falling back to it broke the ollama_client import in compat.py when CHEAP_LLM_HOME was exported. Now WEB_RESEARCH_SCRIPTS or ~/.claude/scripts only. E2E proven: CHEAP_LLM_HOME set → both modules still resolve. (2) shared/compat.py — adopted the cheap_llm contract version-gate: `_CHEAP_LLM_MIN_VERSION = "1.1"` + `cheap_llm.require(...)` after import, fails fast on drift instead of cryptic mid-run error. Regressions: pytest 63 passed, ruff clean.
- 2026-07-08T00:49:42Z | status:completed | Memory hygiene: replaced compact-session directives in activeContext with stable web-research role/context for cross-CLI agents.
- 2026-07-08T02:08:13Z | status:completed | 2026-07-08: Local synthesis default changed to web_synth #1 aratan/gemma-4-E4B-it-heretic:Q6_K. OLLAMA_MODEL remains qwen3.5:4b for query profile/focused extract compatibility; OLLAMA_SYNTH_MODEL remains the override knob for final cited synthesis.
- 2026-07-08T21:30:00Z | status:completed | 2026-07-08: Zero-dependency enhancement batch (11 changes, all stdlib, backward-compatible). Resilience: HTTP retry/backoff in UrllibHttpClient (429/5xx/URLError, env WEB_RESEARCH_HTTP_RETRIES/BACKOFF); cache size-bound LRU eviction (WEB_RESEARCH_CACHE_MAX_ENTRIES/BYTES). Compliance: robots.txt gate (shared/robots.py, fail-open, --no-robots flag). Ranking: authority domains → features/ranking/data/authority_domains.txt via importlib.resources; query_word_overlap stopword/punct-aware; optional TEI cross-encoder stage-2 (features/ranking/tei_rerank.py, enabled by TEI_RERANK_URL, no-op when unset — Ollama has no native /rerank in 2026). Reader: stdlib HtmlReader backend (read/backends/html.py) as zero-dep last-resort, appended to fallback chain. Search: DuckDuckGo zero-dep backend (search/backends/duckduckgo.py, unwraps /l/?uddg redirect). Synthesis: tolerant _extract_json_object (brace-balanced, prose/fence-tolerant). CLI: --engine duckduckgo (search/research), --engine html + --no-robots (read). Deliberately NOT added (YAGNI/zero-dep): asyncio, instructor, diskcache, trafilatura, tenacity. Verified: ruff check + format clean (47 files), mypy 0 issues (43 files), pytest 93/93 (was 65). Files: 11 modified (+386/-67), 7 new incl CHANGELOG.md + tests/test_enhancements.py (28 tests).

- 2026-07-08T22:10:00Z | status:completed | 2026-07-08: Second-pass polish + ecosystem analysis. (1) fix(http): honor Retry-After on 429/503 (cap 30s). (2) refactor(json): extracted tolerant JSON parser to shared/json_utils.py, reused by synthesis + intelligence (removes duplicated fragile parse in query_profile). (3) test: hardened QueryProfileRuleTests (is_alive cache bust in setUp) + added ReaderChainTests (Firecrawl->HTML fallback) + Retry-After unit tests; suite now 96 green, ruff+mypy clean. (4) docs(ecosystem): added docs/ECOSYSTEM.md (coupling map: ollama_client flat script consumed by 4 projects; cheap_llm graduated; 7 divergent model defaults) + docs/proposals.md (P1 graduate ollama_client, P2 shared model registry, P3 shared LLM cache, P4 web-research<->codeq, P5 unified harness_path). Cross-repo proposals await scope confirmation.
- 2026-07-08T23:55:00Z | status:completed | 2026-07-08: P1 CLOSED end-to-end (autonomous). smart-trim + prompt-improve migrated to `require('1.0')` (branches pushed to github); codeq deferred — its `shared/llm.py` uses lazy in-function imports with its own error-return tuple, so the gate is a small design decision (helper vs per-call), not the 1-line change the others were; the shim keeps codeq fully functional. `~/ollama-client` pushed PUBLIC → github.com/heldigard/ollama-client (gitleaks clean pre-push). `~/.claude` shim FF-merged to main local (no remote on ~/.claude). web-research `feat/zero-dep-enhancements` pushed. Ecosystem asymmetry resolved: ollama_client is now a graduated SemVer project like cheap_llm, with 3/4 consumers on the real contract and the 4th (codeq) held safely by the shim.
- 2026-07-09T00:10:00Z | status:completed | 2026-07-08: Second-pass verification (autonomous). Full battery green: pytest 108/108, ruff clean, mypy 0 issues, ruff format clean (45 files), gitleaks 0. **P4 `--code-analyze` wire verified E2E without network**: `_build_docs(code_analyze=True)` prepends `## Local code context (codeq)` (rerank_results · 18 refs) to `doc.extracted` (what synthesis consumes); `code_analyze=False` control → section absent. CLI also ran live (DuckDuckGo, Scraped 1/1, no crash). **P1 verified across all touch points**: ollama-client module+CLI+shim; ~/.claude main holds shim (FF); 3/4 consumers on `require('1.0')` contract; codeq held by shim (loads 1.0.0). Cleaned regenerable caches. ollama-client tree normalized (CLAUDE.md rewritten from boilerplate to real; uv.lock committed; .claude/.claudeignore ignored) + pushed (97bbd79). **DEBT FOUND (preexisting, NOT this session)**: `codescan dead` flags 4 unreferenced symbols in `shared/http.py` (`_http`, `_get_json`, `_post_json`, `set_default_client`) — confirmed via `codeq refs` (0 refs repo-wide). Left untouched: `set_default_client` is the documented future-httpx swap point; the 3 helpers are convenience YAGNI. Logged for a separate cleanup pass (do not mix with feature work).
- 2026-07-09T00:25:00Z | status:completed | 2026-07-09: Debt cleanup (user: "dejar la solución limpia y sin deuda"). Removed the 4 dead symbols from `shared/http.py` confirmed via `codeq refs` (0 callers repo-wide, src + tests): `set_default_client` (aspirational swap setter never called — tests mock `urllib.request.urlopen` instead), `_http` / `_get_json` / `_post_json` (legacy back-compat helpers whose "kept for call sites" comment was stale — the backend-split refactor migrated every call site to `default_client()`), and the `_encode_query` alias. **Retained** `default_client` (every backend resolves through it) + `_warn`/`_debug`/`urlencode` (live callers in synthesis/research/read/backends). `ARCHITECTURE.md` swap recipe rewritten honestly (edit the `_client` assignment, not a dead setter). `codescan dead` drops 28 → 24; the residual 24 are confirmed false positives by construction (HTMLParser framework callbacks, config-surface settings read via the `Settings` proxy, test-only `_bust_alive_cache`/`bust_cache` helpers) — static-analysis limits, NOT debt. Suite 108/108, ruff + mypy clean.
- 2026-07-09T21:32:46Z | status:completed | 2026-07-09: research --json now emits schema-versioned answer/evidence/source provenance with generated time, cache hit, published dates, and scrape success.
- 2026-07-10T18:36:31Z | status:completed | 2026-07-10: Router capability manifest is compact JSON by default and accepts --json for uniform callers. Optional cheap-llm compatibility now uses explicit importlib/getattr typing, preserving graceful fallback while Pyright remains clean. Cache writes remain atomic and HTTP URL validation intact.
- 2026-07-12T08:10:09-05:00 | status:completed | Autonomous project audit shipped locally on `feat/zero-dep-enhancements`: corrected independent cache budget semantics (`0` disables one axis), true mtime-backed LRU without TTL extension, deterministic eviction, and no eviction after failed writes; isolated search rerank and read robots/timeout cache variants; normalized untrusted LLM query profiles while preserving valid empty site lists; made common CLI settings reentrant across embedded calls; refreshed README/CLAUDE/CHANGELOG; enriched schema-v1 capability cards with additive machine-readable options. Review found/fixed one empty-list normalization regression and no remaining critical/important issues. Validation: 125/125 tests, 89.30% coverage, Ruff format+lint clean, mypy entire repo 0, compile/build/smokes green, codescan all 0 secrets/SAST/dead/lint/type/arch violations (dependency-cruiser skipped as non-applicable). Plan: `docs/plans/2026-07-12-001-fix-cache-cli-resilience-plan.md`.
